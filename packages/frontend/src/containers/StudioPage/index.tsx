import React from 'react';

import DefaultTemplate from 'components/templates/DefaultTemplate';

export default function StudioPage(): JSX.Element {
  return <DefaultTemplate article={<article>studio</article>} />;
}
